version: '3.8'

services:
  complex-network-sim:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3006:3006"
    environment:
      - PORT=3006
      - NODE_ENV=production
    restart: unless-stopped
